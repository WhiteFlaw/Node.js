服务端渲染 & 客户端渲染

router是express中自带的路由对象

app.use()中的不一定run, 要看路径是否匹配;

想让页面呈现更快就使用服务端渲染, 直接将渲染后的页面发给前端,前端只需要做一个模板就好了, 缺点就是代码难以维护, 前后端工作量失衡.

前端不直接使用静态资源目录, 前后端分离, 前端从后端请求接口

前端请求接口, 后端发了个json, 前端去解析json, 用json去填充模板

express自带的方法express.static('静态资源目录路径');